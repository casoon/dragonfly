---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Grid-System">
  <h1>Grid-System</h1>
  
  <section class="demo-section">
    <h2>Verwendete CSS-Imports</h2>
    <div class="demo-item">
      <h3>Grid-System</h3>
      <code>@import '../styles/layout/grid.css';</code>
      <p>Enthält das komplette Grid-System mit Spalten, Spannen und Ausrichtung.</p>
    </div>

    <div class="demo-item">
      <h3>Responsive Grid</h3>
      <code>@import '../styles/responsive.css';</code>
      <p>Enthält responsive Anpassungen für verschiedene Bildschirmgrößen.</p>
    </div>

    <div class="demo-item">
      <h3>Utility-Klassen</h3>
      <code>@import '../styles/utilities.css';</code>
      <p>Enthält Hilfsklassen für Abstände und Ausrichtung.</p>
    </div>
  </section>

  <section class="demo-section">
    <h2>Grundlegende Grids</h2>
    <p>Einfache Grid-Layouts mit verschiedenen Spaltenanzahlen.</p>
    
    <div class="demo-grid">
      <div class="grid cols-1 gap-4 demo-item">
        <div class="demo-box">1 Spalte</div>
        <code>&lt;div class="grid cols-1"&gt;...&lt;/div&gt;</code>
      </div>
      
      <div class="grid cols-2 gap-4 demo-item">
        <div class="demo-box">Spalte 1</div>
        <div class="demo-box">Spalte 2</div>
        <code>&lt;div class="grid cols-2"&gt;...&lt;/div&gt;</code>
      </div>
      
      <div class="grid cols-3 gap-4 demo-item">
        <div class="demo-box">Spalte 1</div>
        <div class="demo-box">Spalte 2</div>
        <div class="demo-box">Spalte 3</div>
        <code>&lt;div class="grid cols-3"&gt;...&lt;/div&gt;</code>
      </div>
    </div>
  </section>

  <section class="demo-section">
    <h2>Responsive Grids</h2>
    <p>Grids, die sich an verschiedene Bildschirmgrößen anpassen.</p>
    
    <div class="demo-grid">
      <div class="grid cols-1 sm:cols-2 md:cols-3 gap-4 demo-item">
        <div class="demo-box">1 Spalte (xs)</div>
        <div class="demo-box">2 Spalten (sm)</div>
        <div class="demo-box">3 Spalten (md)</div>
        <code>&lt;div class="grid cols-1 sm:cols-2 md:cols-3"&gt;...&lt;/div&gt;</code>
      </div>
      
      <div class="grid cols-1 md:cols-2 lg:cols-4 gap-4 demo-item">
        <div class="demo-box">1 Spalte (xs)</div>
        <div class="demo-box">2 Spalten (md)</div>
        <div class="demo-box">4 Spalten (lg)</div>
        <div class="demo-box">4 Spalten (lg)</div>
        <code>&lt;div class="grid cols-1 md:cols-2 lg:cols-4"&gt;...&lt;/div&gt;</code>
      </div>
    </div>
  </section>

  <section class="demo-section">
    <h2>Spalten-Spannen</h2>
    <p>Elemente über mehrere Spalten verteilen.</p>
    
    <div class="demo-grid">
      <div class="grid cols-3 gap-4 demo-item">
        <div class="col-span-1 demo-box">Spalte 1</div>
        <div class="col-span-2 demo-box">Spalte 2+3</div>
        <code>&lt;div class="col-span-2"&gt;...&lt;/div&gt;</code>
      </div>
      
      <div class="grid cols-4 gap-4 demo-item">
        <div class="col-span-2 demo-box">Spalte 1+2</div>
        <div class="col-span-2 demo-box">Spalte 3+4</div>
        <div class="col-span-4 demo-box">Alle Spalten</div>
        <code>&lt;div class="col-span-4"&gt;...&lt;/div&gt;</code>
      </div>
    </div>
  </section>

  <section class="demo-section">
    <h2>Responsive Spalten-Spannen</h2>
    <p>Spalten-Spannen für verschiedene Bildschirmgrößen.</p>
    
    <div class="demo-grid">
      <div class="grid cols-1 md:cols-3 gap-4 demo-item">
        <div class="col-span-1 md:col-span-2 demo-box">1 Spalte (xs), 2 Spalten (md)</div>
        <div class="col-span-1 demo-box">1 Spalte</div>
        <code>&lt;div class="col-span-1 md:col-span-2"&gt;...&lt;/div&gt;</code>
      </div>
    </div>
  </section>

  <section class="demo-section">
    <h2>Grid-Ausrichtung</h2>
    <p>Elemente innerhalb des Grids ausrichten.</p>
    
    <div class="demo-grid">
      <div class="grid cols-3 gap-4 items-start demo-item">
        <div class="demo-box">Oben ausgerichtet</div>
        <div class="demo-box">Oben ausgerichtet</div>
        <div class="demo-box">Oben ausgerichtet</div>
        <code>&lt;div class="grid cols-3 items-start"&gt;...&lt;/div&gt;</code>
      </div>
      
      <div class="grid cols-3 gap-4 items-center demo-item">
        <div class="demo-box">Mittig ausgerichtet</div>
        <div class="demo-box">Mittig ausgerichtet</div>
        <div class="demo-box">Mittig ausgerichtet</div>
        <code>&lt;div class="grid cols-3 items-center"&gt;...&lt;/div&gt;</code>
      </div>
      
      <div class="grid cols-3 gap-4 items-end demo-item">
        <div class="demo-box">Unten ausgerichtet</div>
        <div class="demo-box">Unten ausgerichtet</div>
        <div class="demo-box">Unten ausgerichtet</div>
        <code>&lt;div class="grid cols-3 items-end"&gt;...&lt;/div&gt;</code>
      </div>
    </div>
  </section>

  <section class="demo-section">
    <h2>Grid-Utility-Klassen</h2>
    <p>Kontrollieren Sie das Grid-Layout mit diesen Utility-Klassen.</p>
    
    <div class="demo-grid grid cols-1 md:cols-2 gap-4">
      <div class="demo-item">
        <h3>Spalten</h3>
        <ul>
          <li><code>cols-1</code> bis <code>cols-12</code> - Spaltenanzahl</li>
          <li><code>sm:cols-*</code> - Spalten für kleine Bildschirme</li>
          <li><code>md:cols-*</code> - Spalten für mittlere Bildschirme</li>
          <li><code>lg:cols-*</code> - Spalten für große Bildschirme</li>
        </ul>
      </div>
      <div class="demo-item">
        <h3>Spannen</h3>
        <ul>
          <li><code>col-span-1</code> bis <code>col-span-12</code> - Spalten spannen</li>
          <li><code>sm:col-span-*</code> - Spannen für kleine Bildschirme</li>
          <li><code>md:col-span-*</code> - Spannen für mittlere Bildschirme</li>
          <li><code>lg:col-span-*</code> - Spannen für große Bildschirme</li>
        </ul>
      </div>
      <div class="demo-item">
        <h3>Ausrichtung</h3>
        <ul>
          <li><code>items-start</code> - Oben ausrichten</li>
          <li><code>items-center</code> - Mittig ausrichten</li>
          <li><code>items-end</code> - Unten ausrichten</li>
          <li><code>justify-start</code> - Links ausrichten</li>
          <li><code>justify-center</code> - Zentrieren</li>
          <li><code>justify-end</code> - Rechts ausrichten</li>
        </ul>
      </div>
      <div class="demo-item">
        <h3>Abstände</h3>
        <ul>
          <li><code>gap-1</code> bis <code>gap-8</code> - Spaltenabstand</li>
          <li><code>gap-x-*</code> - Horizontaler Abstand</li>
          <li><code>gap-y-*</code> - Vertikaler Abstand</li>
        </ul>
      </div>
    </div>
  </section>
</Layout>

<style>
  .demo-section {
    margin: var(--size-8) 0;
    padding: var(--size-4);
    border: 1px solid var(--gray-3);
    border-radius: var(--radius-2);
  }

  .demo-grid {
    display: grid;
    gap: var(--size-4);
    margin: var(--size-4) 0;
  }

  .demo-item {
    padding: var(--size-4);
    background: var(--gray-1);
    border-radius: var(--radius-2);
  }

  .demo-box {
    padding: var(--size-4);
    background: var(--gray-2);
    border-radius: var(--radius-1);
    text-align: center;
  }

  .demo-item code {
    display: block;
    margin-top: var(--size-2);
    padding: var(--size-2);
    background: var(--gray-2);
    border-radius: var(--radius-1);
    font-family: var(--font-mono);
    font-size: var(--font-size-0);
  }

  .demo-item h3 {
    margin: 0 0 var(--size-2) 0;
    font-size: var(--font-size-2);
  }

  .demo-section h2 {
    margin-bottom: var(--size-4);
  }

  .demo-section p {
    margin-bottom: var(--size-4);
    color: var(--gray-7);
  }

  .demo-item ul {
    text-align: left;
    margin: 0;
    padding-left: var(--size-4);
  }

  .demo-item li {
    margin: var(--size-2) 0;
  }
</style> 